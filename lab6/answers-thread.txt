通过分析源代码可知，导致丢失的发生是在put函数位置。
假设现在有两个线程T1和T2，两个线程都走到put函数，且假设两个线程中key%NBUCKET相等，即要插入同一个散列桶中。
两个线程同时调用insert(key, value, &table[i], table[i])，insert是通过头插法实现的。
如果先insert的线程还未返回另一个线程就开始insert，那么前面的数据会被覆盖，从而造成键的丢失。
而由于T1,T2两个线程执行的顺序不确定，因此两个线程都有可能丢失数据。
而对于get操作，由于它不需要对数据进行修改，只是读取数据，则该操作对于多线程来说是没有问题的。
